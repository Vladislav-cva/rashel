import { createPath } from "rd-url-utils";

export const MAIN_PAGE_URL = createPath<{}>("/main");
export const CLIPS_URL = createPath<{}>("/clips");
export const CONTACTS_URL = createPath<{}>("/contacts");
export const MUSIC_URL = createPath<{}>("/music");
export const NEWS_URL = createPath<{}>("/news");
export const GALLERY_URL = createPath<{}>("/gallery")


